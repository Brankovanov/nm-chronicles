import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-city',
  imports: [RouterLink],
  templateUrl: './city.html',
  styleUrls: ['./city.scss'],
})
export class City {}
